__pycache__/
*.pyc

# PyInstaller build intermediates
build/
dist/
*.spec

# Packaged releases (built locally, distributed via GitHub Releases instead
# of committed to git - they're large compiled binaries, not source)
RampartChessWindows/
RampartChessLinux/
*.tar.gz
*.tar.xz
*.apk

# runtime debug log
launch_error.log

# Firebase Admin SDK service-account credentials - never commit these
serviceAccountKey.json
*firebase-adminsdk*.json
server/resend_key.txt
server/resend_key.txt

# Cloudflare Access test credentials + debug traces - never commit
server/cloudflare_testing.txt
server/debug_trace.txt
server/mcts_trace.txt
