# Never commit signing material or build outputs (keystore lives OUTSIDE
# the repo in ~/Documents/rampart_secrets/ - these patterns are a safety net).
*.jks
*.keystore
*.p12
keystore.properties
*.aab
*.apk
*.zip
